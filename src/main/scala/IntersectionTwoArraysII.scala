package leetcode

//object IntersectionTwoArraysII extends App {
//  def intersect(nums1: Array[Int], nums2: Array[Int]): Array[Int] = {
//     if(nums1.isEmpty) Array.empty
//     else if(nums2.isEmpty) Array.empty
//     else {
//       val freq1 = nums1.groupBy(a => a).map{case (a, arr) => (a, arr.length)}
//       var map = Map.empty[Int, Int]
//       var i = 0
//       while(i < nums2.length){
//           val n = nums2(i)
//           val c = freq1.getOrElse(n, 0)
//
//       }
//       freq1.toList.int
//       freq1.foldLeft(freq2){(res, kv) =>
//
//       }
//     }
//  }
//}
